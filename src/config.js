var config = {
	// log level 0-3
	debug: 3,

	server: {
		// ip address of interface to listen on
		// leave 0.0.0.0 to listen on all interfaces
		host: '0.0.0.0',

		// port to listen on
		port: 9000
	}
};

module.exports = config;